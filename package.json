{
  "name": "wetransport",
  "version": "1.0.0",
  "private": true,
  "description": "Organizza i passaggi in macchina della comitiva. Sito statico: queste dipendenze sono strumenti di sviluppo, non servono per servire l'app — supabase-js c'e' solo perche' `npm run vendor` la impacchetta in vendor/, che e' cio' che il browser carica.",
  "scripts": {
    "lint": "eslint app.js mod config.js sw.js rete.js accesso.js tema.js",
    "validate": "html-validate index.html privacy.html termini.html 404.html offline.html",
    "test:e2e": "playwright test",
    "contrasto": "node tests/contrasto.mjs",
    "moduli": "node tests/moduli.mjs",
    "auto": "node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON tests/auto.mjs",
    "immagini": "node immagini.mjs",
    "check": "npm run lint && npm run validate && npm run moduli && npm run contrasto",
    "vendor": "esbuild node_modules/@supabase/supabase-js/dist/index.mjs --bundle --format=esm --minify --target=es2020 --legal-comments=none --platform=browser --outfile=vendor/supabase-js.esm.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "@supabase/supabase-js": "^2.116.0",
    "esbuild": "^0.28.2",
    "eslint": "^10.8.0",
    "html-validate": "^11.5.6"
  },
  "engines": {
    "node": ">=22"
  }
}
